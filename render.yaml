services:
  - type: web
    name: sentconnect.org
    plan: free
    runtime: node
    region: oregon
    branch: main
    buildCommand: npm install -g pnpm && pnpm install --include=dev && pnpm --filter @workspace/api-server run build && BASE_PATH=/ pnpm --filter @workspace/sentconnect-slides run build
    startCommand: pnpm --filter @workspace/api-server run start
    healthCheckPath: /api/healthz
    envVars:
      - key: NODE_ENV
        value: production
      - key: SESSION_SECRET
        generateValue: true
